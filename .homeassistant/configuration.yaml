default_config:

recorder:
  db_url: sqlite:////tmp/home-assistant_v2.db
  purge_keep_days: 3
  exclude:
    entity_globs:
      - sensor.yarbo_*

logger:
  default: warning
  logs:
    custom_components.yarbo: info
